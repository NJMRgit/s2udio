# Session log — 2026-08-09

## Distro support — Phase 4 review follow-up (setup.sh dispatcher)

Host review of commit 8f50330 (setup.sh dispatcher) landed two fixes:

1. **Privilege elevation (non-Arch backends)**: new shared
   `resolve_elevation()` helper sets `ELEVATE_CMD` once per runner —
   root → no prefix; else `sudo`; else `doas` (Alpine); else warn+die
   ("cannot install packages — re-run as root or install sudo/doas").
   All elevation calls in the dnf5/apt/apk/xbps/nix runners converted
   (`"${ELEVATE_CMD[@]}" dnf5/apt-get/apk/xbps-install/...`); the Arch
   path's literal `sudo` untouched (byte-identity kept). Array form so the
   root case yields zero words (a quoted empty string would be an empty
   command). Mock matrix grew 4 cases: root+no-sudo → direct installs;
   non-root+sudo → sudo prefix; non-root+doas → doas prefix;
   non-root+neither → clear die, no partial install. Real Alpine run with
   the harness sudo shim omitted (`--no-sudo`) → all gates green, direct
   root installs, G12 clean.

2. **Stale comment in run_arch**: `~/.cache/rmpc/mpris-art` →
   `~/.cache/s2udio/mpris-art` in the mpDris2 drop-in comment (2
   occurrences of the same line). Fixture regenerated from the updated
   run_arch: `scripts/dev/fixtures/setup.sh.arch-baseline` = master's
   pre-dispatcher setup.sh (49c23b8) + exactly this one comment line
   (noted in the fixture header); mock byte-identity still green.

Mock matrix now **50/50**; container runs fedora-41/debian-12/alpine-320
(± `--no-sudo`) all green; zero `s2u-distro-*` leftovers.

## Round 23 — configs fully in ~/.config/s2udio + s2udio-owned lyrics library (host)

User feedback (2026-08-09, direct to the host): move ALL configs to
`~/.config/s2udio` (nothing left in `~/.config/rmpc`), and make the
lyrics directory `~/.config/s2udio/lyrics` — s2udio maintains its own
.lrc library; the user's MPD library is checked first and never
overwritten.

**Changed** (host, on `working`):

1. **Config layout** (`src/shared/paths.rs`): `config_dir()` now returns
   `~/.config/s2udio` (was `~/.config/rmpc`); `config_paths()` =
   `[~/.config/s2udio/config.ron, ~/.config/rmpc/config.ron (legacy),
   ~/rmpc/config.ron (legacy)]`; theme resolution s2udio-first with a
   legacy rmpc fallback.
2. **One-time migration** (`src/shared/config_read.rs`): new
   `resolve_config_path()` — when only a legacy config exists, merge the
   legacy base + the round-19 `~/.config/s2udio/config.ron` overlay into
   one full `~/.config/s2udio/config.ron` (ron round-trip), copy legacy
   sidecars/themes over, rename the legacy base
   `config.ron.migrated-round23` so the merge never re-runs. Overlay
   files are detected by their top-level keys (only the five s2udio
   sections). A broken overlay is warned-and-ignored (see below). The
   round-19 overlay merge is retired from the hot-reload/read path.
3. **Sidecar legacy fallbacks** (`state.rs`, `keys/mod.rs`, `cava.rs`,
   `jellyfin.rs`): repointed from `config_dir()` to the new
   `legacy_config_dir()` (`~/.config/rmpc`).
4. **Defaults**: `lyrics_dir` default → `~/.config/s2udio/lyrics`
   (example_config.ron; the old `/mnt/20TBHDD/Media/Music` personal path
   is gone from the shipped config); torrent `cache_dir` default →
   `~/.cache/s2udio/torrents`.
5. **Lyrics lookup** (`src/ctx.rs` `find_current_lyrics_path`): 1) the
   user's MPD library colocated `.lrc` (absolute song paths stand alone;
   relative paths resolve via `paste::music_directory()` reading
   mpd.conf) — READ-ONLY, checked even without a configured
   `lyrics_dir`; 2) s2udio's own library colocated mirror; 3) the s2udio
   index (metadata match). The write path (`LRC_FILE` env /
   `rmpc-fetch-lyrics`) targets only `lyrics_dir`, so user library files
   are never overwritten.
6. **Lyrics dir auto-create** (`src/core/lyrics_watcher.rs` +
   `src/main.rs`): `create_dir_all` instead of bailing on a missing dir
   (fresh installs get hot reload + indexing right away).
7. **Scripts/setup**: `s2u-mpv-tracker` reads
   `~/.config/s2udio/config.ron` first (legacy fallback) for MPD
   address/password and the jellyfin sidecar; `setup.sh` seeds
   `~/.config/s2udio/` (config + theme + an empty `lyrics/`).
8. **Tests**: +2 — `lyrics_lookup_prefers_user_library_then_s2udio_
   library_then_index` and
   `migration_merges_legacy_base_and_overlay_into_s2udio_config`
   (shared `HOME_LOCK` added in `src/tests/fixtures/mod.rs` to serialize
   HOME-mutating tests). **1282/1282**, warnings 3 baseline.
9. **Repo cleanup (user)**: the upstream-rmpc leftovers were removed —
   `CHANGELOG.md`, `CONTRIBUTING.md`, `FAQ.md`, the placeholder art
   (`music/radio/video/preview` jpgs/pngs) and `assets/rmpc.desktop`.
   Kept: `assets/default.jpg` (compiled in as the default album art via
   `include_bytes!`) and the example configs.

**Live migration (this host)**: merged configs → full
`~/.config/s2udio/config.ron` (base + overlay sections; overlay had
never actually parsed — see below), sidecars/themes already present in
`~/.config/s2udio/`, legacy `~/.config/rmpc` renamed aside to
`~/.config/rmpc.migrated-round23` (kept as a backup, upstream rmpc is
not installed here). 171 s2udio-stamped `.lrc` files moved from
`/mnt/20TBHDD/Media/Music` → `~/.config/s2udio/lyrics` (relative layout
preserved; 1788 hand-made files stayed in the library). Caches
(`~/.cache/rmpc/{yt-info,radio-directory,torrents,mpris-art}`) moved to
`~/.cache/s2udio/`.

**Important history found**: the round-19 overlay
(`~/.config/s2udio/config.ron`) had NEVER parsed — ron rejects its `#`
comment lines (`ExpectedAttribute`) and bare `Option` values; the log
shows `Failed to read the s2udio config overlay` since round 19. The
s2udio sections (`radio`/`jellyfin`/`video`/`mpv`/`torrent`) effectively
ran on embedded defaults. The round-23 migration writes one correct full
config, fixing that silently.

**Pending live checks (user restart)**: lyrics display from the new
library; user library .lrc still wins over s2udio copies; config edits
land in `~/.config/s2udio/`.

## Round 22 — picker title marker + right margin (IMPLEMENTED — isodev)

The pull brought `fabbdeb..5bc4d79` (rounds 20–21 + picker-UX
follow-ups + the round-22 feedback filing). The pulled state validates
(**1279/1279**, warnings 3 baseline, tracker 6/6, s2u-mpdris 21/21,
mpdris2-shim 1/1), and round 22 itself was then implemented (the
round was filed "for isodev"; the spec was complete, so it was done
directly in the dev container).

**Changed** (committed on `working`):

1. **Title** (`src/ui/modals/paste.rs`, `open_torrent_file_picker`):
   `format!(" Select files — {} ", scan.torrent_name)` →
   `format!("▶ files — {} ", scan.torrent_name)`. For long names the
   centered title used to overflow and truncate from the LEFT, leaving
   a stray "t" (tail of "Select"); now it truncates cleanly to "▶…".
2. **Right margin** (`src/ui/modals/torrent_file_picker.rs`, render):
   the list block gains `.padding(Padding::new(0, 1, 0, 0))` — one
   blank column between the file rows and the scrollbar
   (`…mkv ↑│` instead of `…mkv↑│`). `options_area` (the click-target
   rect) derives from `list_block.inner(list_area)`, so the rendered
   rows, `get_at_rendered_row` / `options_area.contains`, and the
   scrollbar hit column (last column of `scrollbar_area`) all stay
   consistent with the narrower area.
3. **Tests** (`src/ui/modals/paste.rs`): the 11 picker tests' title
   argument updated to `"▶ files — Fake Pack "`; new
   `torrent_file_picker_round22_title_marker_and_right_margin` renders
   the picker with the long Banana-Fish-style name and asserts the
   title marker + the blank column at x=68 (scrollbar at x=69) on the
   file row.

**Validation**: `cargo test --release` **1280/1280** (+1), binary
warnings **3** (unchanged baseline), stable `cargo fmt --check` clean
(nightly fmt unavailable on this host). Binary md5 `9932fc1f`
installed to `~/.local/bin/s2udio` (was `36d58c60`); no live instance
— user restart picks it up. Tracker 6/6, s2u-mpdris 21/21,
mpdris2-shim 1/1 (untouched this round).

## Round 22 — picker title marker + right margin (feedback FILED for isodev)

User feedback (2026-08-09) → [FEEDBACK-2026-08-09-16.md](../../../FEEDBACK-2026-08-09-16.md).
The picker UX work (rounds 20–21 follow-ups) is user-confirmed working;
two UI nits remain, filed for the container round:

1. **Title stray "t"** — `open_torrent_file_picker` builds
   `format!(" Select files — {} ", scan.torrent_name)`. For long names
   (Banana Fish) the centered title overflows and truncates from the
   LEFT, leaving a stray "t" (tail of "Select") at the start:
   `╭t files — Banana.Fish… ──╮`. Desired: `╭▶ files — … ──╮` — title
   becomes `format!("▶ files — {} ", scan.torrent_name)`.
2. **Right margin** — the picker's list rows run flush to the scrollbar
   column (`…mkv↑`); add a 1-char margin (`…mkv ↑`) by shrinking the
   List area's right side by one column in `torrent_file_picker.rs`
   render (options_area / list-block right padding; scrollbar_area stays).

Host reproduced both live in a render probe (title row exactly
`╭t files — Banana… ──╮`, rows flush to the thumb). No code change this
side — the round is isodev's; host validates after the pull.

## Round 21 — torrent popup options: Stream/Download + download-only actions (host-side, user feedback)

The user gave the round-21 spec directly to the host (not via the
container round): the `[Torrent]` popup's options become, for a
**single-file** torrent, `Stream` / `Download` / `Cancel`; for a
**multi-file** torrent, `Stream all` / `Download all` / `Select files…` /
`Cancel` (the plain single-file action is gone from the multi popup).

**Changed** (all committed host-side on `working`):

1. **Popup labels** (`src/ui/modals/paste.rs`): single-file actions are
   `Stream` (play now — the old "Play (stream)") and `Download`;
   multi-file actions are `Stream all` (the old "Play all (N files)",
   count dropped per the user's spec) and `Download all`; `Select
   files…` and `Cancel` unchanged.

2. **Download-only semantics** (new): `Download` / `Download all` keep
   the file(s) in `~/Downloads/s2udio-downloads` **without playback** —
   distinct from the picker's "Download & Play" (play + keep) and the
   old single-file "Play and Download". The popup's actions send a new
   `AppEvent::TorrentScannedDownload { scan, file_indices }`; the event
   loop keeps the scanned engine (`Ctx.torrent_engine`) and starts the
   download job with no mpv session.

3. **Multi-file download job** (`src/core/torrent.rs`): `TorrentDownload`
   now holds `files: Vec<TorrentDownloadFile>` (index, length, name,
   source path, stream URL) — one job per torrent.
   `download_complete` requires `stats.finished` **and** every kept
   file's `file_progress` at full length; `finish_torrent_download`
   moves every kept file (status per file) then deletes the torrent;
   the poll's deferral check (mpv still playing a kept stream → move on
   `MpvSessionEnded`) now scans **all** kept stream URLs. The picker's
   "Download & Play" now tracks **every** marked file (was the first
   only). The fresh-engine fallback (`WorkRequest::DownloadTorrent` +
   `WorkDone::TorrentDownloadPrepared`) prepares the engine and
   registers the files as a canonical-key scan before starting the job.

4. **Tests** — `cargo test --release` **1275/1275** (round-20 baseline
   1273 + 2 new: `download_action_sends_the_download_only_event_for_the_picked_file`,
   `download_all_sends_the_download_event_with_every_video_index`; the
   single/multi popup label tests reworked to the round-21 strings).
   Binary warnings 3 = baseline. Tracker 6/6, s2u-mpdris 21/21,
   mpdris2-shim 1/1. Binary md5 `79f722b3` installed.

   **Follow-up (user note)**: Enter on the picker's file list no longer
   plays immediately — it moves the cursor to the action buttons
   (Play / Download & Play / Cancel; Play focused), so the user chooses
   after marking (help row now reads "Enter: buttons"). +1 test
   (`picker_enter_on_the_list_moves_to_buttons_then_choose_download_and_play`),
   **1276/1276**; binary md5 `06d0e7d3` installed.

   **Follow-up 2 (user notes)**: the picker is now fully mouse/key friendly:
   (a) `a`/`d` and ←/→ move between the action buttons (the minimal
   keybind set only binds w/s/↑/↓ in navigation, so the picker claims
   the horizontal keys raw on the Buttons focus; configs that bind them
   arrive as CommonAction::Left/Right); (b) buttons are clickable — a
   single click selects + focuses, a **double-click activates**
   Play / Download & Play / Cancel; (c) the scrollbar is mouse-
   interactable (`DirState::scrollbar_drag` — click/drag scrolls, same
   thumb-follow contract as the tabs); (d) the wheel moves the list
   selection over the list (and the button selection over the buttons).
   +3 tests (a/d keys, double-click activation, wheel+scrollbar),
   **1279/1279**; binary md5 `9897cce0` installed.

**Live checks pending user restart**: popup shows Stream / Download /
Cancel (single) and Stream all / Download all / Select files… / Cancel
(multi); Download / Download all land the files in
`~/Downloads/s2udio-downloads` without opening mpv; Stream all plays the
season sequentially.

## Round 20 — torrent popup UI polish + duplicate-paste fix (container)

Feedback: [FEEDBACK-2026-08-09-15.md](../../../FEEDBACK-2026-08-09-15.md)
(pulled at `e195764`, "round 20 feedback filed"). Round 19 (config
separation) is implemented host-side and stays uncommitted in the host's
testing checkout — not touched here. All changes below are in the
container checkout on `working`, uncommitted, for the host to validate
after the pull.

**Changed:**

1. **Wait window — DL-speed row removed** (`src/ui/modals/paste.rs`):
   the `[Torrent]` scan-pending branch now renders only
   `Loading <label>… mm:ss` + `esc to cancel` (+ `Cancel`). The
   `DL <speed> · need ≥ <min> KB/s ✓/✗` header row and the
   `scan_wait_speed` helper are gone (live use showed the row is noise
   during the metainfo wait — speed is ~0 so it always read ✗).
   `TorrentScanProgress.download_speed_kbps` is **kept** in the
   event/`Ctx` structures (the M3 bandwidth gate still reads it); only
   the rendering dropped it.

2. **File picker buttons — Play / Download & Play / Cancel**
   (`src/ui/modals/torrent_file_picker.rs` + `paste.rs`):
   - `TorrentFilePicker` gained a `TorrentPickerAction` (`Play` /
     `DownloadAndPlay`) argument on the confirm callback; the button row
     is now `Play` · `Download & Play` · `Cancel` (button 0 / 1 / 2).
   - `open_torrent_file_picker` passes the action through: `Downloads &
     Play` routes `AppEvent::TorrentScannedPlay { download: true }` →
     the existing "Play and Download" machinery in `start_torrent_playback`
     (engine download job + move to `~/Downloads/s2udio-downloads`).
   - The round-18 fix is preserved: the picker **captures the scan when
     it opens** (now a clone, see below) — both buttons play from the
     captured scan, no re-lookup.

3. **Duplicate-paste bug** (paste same magnet twice → error / won't
   load on the repeat):
   - **Canonical scan keys**: magnets are keyed by their **full
     infohash** (`magnet_infohash_full`, lowercased) in
     `torrent_item_key` and `TorrentItem::source_key` (they must agree —
     the work thread reports scan results under `source_key`), so the
     same torrent pasted twice — even via a different magnet URI (extra
     trackers) — reuses one scan slot. `parse_paste`'s in-paste magnet
     dedupe also uses the full infohash now.
   - **Engine sharing via `Arc`**: `TorrentScan.engine` is
     `Arc<TorrentEngine>`; `Ctx.torrent_engine` is
     `Option<Arc<TorrentEngine>>`. Playback clones the Arc (the scan map
     keeps its own clone), so the engine survives playback and stays
     reusable.
   - **Landed scans survive the popup**: the paste popup's close hook
     no longer clears `Ctx.torrent_scans` — it keeps `Ok` scans (engine
     alive for reuse) and drops `Err` scans (re-paste retries cleanly).
     `play_scanned_or_fresh` and the picker no longer consume the scan
     (they clone it into the event).
   - **Fresh `PlayTorrent` fallback** registers a single-file scan under
     the item's canonical key (`WorkDone::TorrentStreamPrepared` gained a
     `key` field) so a re-paste of a fallback-played torrent also reuses
     the engine.
   - Root cause per host analysis: re-adding the same magnet while the
     first engine is still alive spawned a second rqbit against the same
     `cache_dir` (file/port conflicts, or the scan result for the same
     key being overwritten). With reuse, no second engine is ever
     spawned for the same torrent. (Real-engine smoke in the container:
     adding the same `.torrent` to a fresh engine with existing partial
     cache files returns 200 and the torrent goes `live` — the
     clean-rescan path is sound; the bug was the *concurrent* second
     engine.)

**Tests** — `cargo test --release` **1273/1273** (round-19 baseline
1268 + 5 new: canonical-key test, landed-scan reuse, in-flight reuse,
picker button labels, picker Download & Play download flag; the two
round-18 wait-window tests reworked to assert the trimmed window).
Binary warnings **3** (unchanged baseline: `config/mod.rs` unused_mut,
`language.rs` unused ctx, `paste.rs` AddAfterCurrent). Tracker 6/6,
s2u-mpdris 21/21, mpdris2-shim 1/1.

**Docs**: `docs/design/Backend/torrent-streaming.md` (wait window +
§9.1 engine reuse & scan identity), `docs/design/Backend/paste-pipeline.md`
(canonical keys), this session log.

**Interim lifecycle note (for M4)**: engines of scanned-but-unplayed
torrents now stay alive (and keep downloading) until app exit — the
full keep/cleanup policy remains M4.

## Distro support — Phase 0 + Phase 1 (harness + Fedora/Debian/Ubuntu)

Plan: `docs/design/Validation/distro-support.md` (committed `ad18e61`).
Executed in ephemeral rootless podman containers (`s2u-distro-*` prefix;
zero containers remain — G12 enforced on every exit).

**Delivered** (committed `5bda69d` on `working`):

1. `scripts/dev/test-distro.sh <key> [--gate] [--no-cache-vol] [--artifacts]`
   — the §4.1 driver: sweep → create (`--systemd=always` +
   `/sbin/init`, `--rm`) → copy repo (no `.git`/`target/`) → provision →
   build + `cargo test --release` → deploy → gates G1–G11 (in-container,
   per-gate JSON) → collect → teardown → G0/G12 host-side. Ephemerality
   4 ways: `--rm`, EXIT trap, start sweep, G12 end assertion.
2. `scripts/dev/containers/<key>/`: init Dockerfiles (systemd as PID 1 —
   the stock base images ship no systemd), `image.env`,
   `provision.sh` (package maps + rustup), `deploy.sh` +
   `containers/common/deploy-common.sh` (setup.sh-equivalent steps).
3. `scripts/dev/gates/run-gates.sh` + `common.sh`: G1–G11.
4. `scripts/s2u-svc`: init abstraction v1 (systemd-user + plain-launcher;
   runit/s6/openrc/sysvinit stubs for P3).
5. Artifacts per run: `scripts/dev/artifacts/<key>/<ts>/` (run.log +
   gate JSONs + gates.jsonl; gitignored).

**Results — all three Phase 1 targets green on G0–G6/G8–G12; G7 soft:**

- fedora-41: 12/12 (G7 pass; yt-dlp 2025.06.30)
- debian-12: 11/12 + G7 soft (yt-dlp 2023.03.04 too old for android_vr)
- ubuntu-2404: 11/12 + G7 soft (yt-dlp 2024.04.09 too old)
- `cargo test --release` **1282/1282** on all three (host baseline intact),
  warnings 3 = baseline.

**Decisions recorded** (plan §5/§6): Fedora mpd comes from RPM Fusion
(Fedora dropped mpd from official repos — AUR-analogue); Debian/Ubuntu
MPD system-vs-user resolved as **user-level** (system unit stopped +
disabled, `~/.config/mpd/mpd.conf` + user unit — the s2udio model);
rustup everywhere (distro rustc < 1.88 cannot build edition-2024);
mpv-full stays Arch-only; mpDris2 user unit provided on Fedora (package
ships none).

**Container gotchas found**: git "dubious ownership" on the copied tree
(fix: `safe.directory` + fresh `git init` for vergen — the build FAILS
without a git repo); MPD needs explicit `db_file`/`state_file` paths
("No database" otherwise); cava's `method = terminal` is invalid
(raw/ncurses only); the tracker's mpv<->MPD mutual exclusion pauses a
video while MPD audio plays (G8 must stop MPD first); mpDris2 picks new
songs up on its own poll cadence (gate polls Metadata).

**Remaining**: P2 (Nix flake + `ghcr.io/nixos/nix` + NixOS module VM
documented, not run), P3 (Alpine/OpenRC → Void/runit: s2u-svc backends +
tracker rewiring), setup.sh dispatcher refactor (package maps + step
deltas delivered as the per-target provision/deploy scripts; the single
`setup.sh` dispatcher remains a follow-up), README install-matrix.

## Distro support — Phase 2 + Phase 3 (Nix, Alpine/OpenRC, Void/runit)

Continuation of the distro-support harness work (Phases 0+1 above).

**Phase 2 — Nix: ALL 12 GATES GREEN** (`artifacts/nix/20260809T161746Z`).
`flake.nix`: package (`rustPlatform.buildRustPackage`, `preBuild` git
snapshot for vergen, scripts+assets installed, `s2udio` alias),
`bridgePython` package (dbus-python, pygobject3, mutagen, python-mpd2),
devShell (rust + mpd/mpv/yt-dlp/cava/mpdris2/ffmpeg + python env).
Validated in the `ghcr.io/nixos/nix` container (plain mode): profile
install + the full G1–G11 loop via the **plain-launcher** backend.
Findings: nixpkgs' mpDris2 is a compiled ELF → upstream python source
installed at /usr/bin/mpDris2 (plan §5 decision point); nix sandbox
lacks network/HOME for ~26 env-dependent tests → `doCheck=false` (suite
still runs via G3/`nix develop`); nix profile install refuses to upgrade
same-name entries → remove-before-install. NixOS module + nixosTest VM:
out-of-container (documented; VM required).

**Phase 3 — non-systemd init: Alpine 3.20 + Void glibc, ALL 12 GATES
GREEN each.** Alpine (`artifacts/alpine-320/20260809T165644Z`): OpenRC
distro validated via the plain-launcher (plan §6.1 design decision);
cava absent from 3.20 → built from source; mpdris2 absent → upstream
source + pip python-mpd2; rustup. Void (`artifacts/void-glibc/
20260809T174208Z`): **runit-user** backend — per-user `runsvdir
~/.config/runit` + `sv(1)` through s2u-svc; distro rustc 1.97.1;
mpDris2 from the Void repos (python source). Void quirks: mpd file caps
(container bounding set) → `setcap -r`; xbps ENOBUFS → `--shm-size=512m`;
missing bash/tar/findutils/coreutils/setsid in the base image.

**Harness hardening across all targets**: MPRIS probes moved from
`busctl` (systemd-only tool) to python-dbus (present everywhere);
G4/G11 backend-aware (systemd-user test unit vs real-mpd round-trip);
G1 script/service checks backend-aware; driver `--shm-size=512m`,
build/test steps no longer mask cargo failures, rustup default
belt-and-braces for the cargo cache volume; s2u-svc runit backend uses
full service paths (no top-level local/return).

**Remaining**: tracker `systemctl --user` rewiring to `s2u-svc`
(document — safe untested on non-systemd), Artix s6/OpenRC + Devuan
stretch targets, setup.sh dispatcher refactor (per-distro package maps +
step deltas delivered as the per-target provision/deploy scripts),
NixOS module + nixosTest VM, README install-matrix section.

## Distro support — Phase 4 (part 1): setup.sh becomes a distro dispatcher

`setup.sh` refactored from the Arch-only 9-step installer into a multi-distro
dispatcher (plan §6.2), driven by the Phase 1–3 validated per-target work
(provision/deploy package maps + step deltas). Detection via
`/etc/os-release` (ID / ID_LIKE): pacman (Arch/CachyOS/Artix — output
byte-for-byte identical, verified), dnf5 (Fedora, RPM Fusion free for
mpd/ffmpeg/mpv), apt (Debian/Ubuntu/Devuan — system mpd stopped+disabled +
user-level instance + stale-yt-dlp pip hint), apk (Alpine — cava from source,
upstream python mpDris2 + pip python-mpd2), xbps (Void — mpd `setcap -r`,
distro rustc 1.97.1 skips rustup), nix (nix profile install, flake.nix).
Shared step functions (support scripts incl. s2u-svc, config/theme seed, MPD
fifo append) stay shared; mpv-full stays Arch-only (others: plain mpv +
informational note); services enable/start through `scripts/s2u-svc` (raw
`systemctl` remains only inside the Arch path). Per-distro deltas: rustup
auto-install when distro rustc < 1.88 (confirm-gated), user-level
`mpd.conf` creation when absent, Debian `ConditionUser=!@system` fallback
unit for root runs, yt-dlp ≥2025.01.01 version check for the §12.7 hint.

Validation:
- `bash -n setup.sh` clean.
- `scripts/dev/test-setup-mock.py` — hermetic mocked matrix (fake pacman/
  dnf5/apt/apk/xbps/systemctl/… in a PATH shim + fake os-release, no host
  toolchain leakage): 44/44 — detection, package names per §5 map, Arch
  byte-identity (-y and no-y vs the frozen `scripts/dev/fixtures/
  setup.sh.arch-baseline`), no installs on non-interactive no -y,
  recommended defaults on -y, unknown distro dies.
- `scripts/dev/test-setup-distro.sh <key>` — real in-container runs of the
  NEW setup.sh (ephemeral harness pattern, G12 discipline): **fedora-41**
  (dnf5 + RPM Fusion + systemd-user services) G0+S1–S8+G12 green;
  **debian-12** (apt + system mpd stopped + ConditionUser fallback)
  G0+S1–S9+G12 green; **alpine-320** (apk + cava source build + upstream
  mpDris2 + launcher services) G0+S1–S8+G12 green. No `s2u-distro-*`
  leftovers; unrelated containers untouched.

Remaining (not attempted, per scope): tracker `systemctl --user` rewiring
to s2u-svc (safe on systemd targets, untested on non-systemd), NixOS module
+ nixosTest VM (out-of-container by design), README install-matrix section,
Artix s6 / Devuan stretch targets. The master-repo `setup.sh` sync is a
separate host decision — NOT done here.
