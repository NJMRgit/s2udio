# Session log — 2026-08-10

## Round 24 — feedback FILED for isodev (host)

User live-test feedback (2026-08-10), filed as
[FEEDBACK-2026-08-10-17.md](../../../FEEDBACK-2026-08-10-17.md):

1. **Esc-deselect everywhere.** The `6056818` fix ("Esc clears first,
   settings on second press") only covered the queue audio list,
   playlists (`browser.rs` ≈602) and search (`search/mod.rs` ≈637).
   Verified missing in the tree at `5e58c29`: the queue **video** list
   (`handle_video_action`, `queue.rs` ≈2590–2665 — `video_marked`
   multi-select exists, no `CommonAction::Close` arm) and the **MPD**
   tab (`directories.rs` `handle_action` ≈1370–1384 — `marked`
   multi-select exists, no Close arm at all). Required: same
   clear + `clear_mark_anchor()` + `event.consume()` pattern so Esc
   deselects first and only a second Esc opens settings; shift-range
   re-anchor rule preserved; pane-level tests mirroring the playlists
   esc/consume + re-anchor tests.

2. **Search tab parity.** The results list is the last list without the
   shared selection/hover interaction: add mouse multi-select
   (ctrl+click toggle, alt+click range, plain-click clear + re-anchor —
   the `queue.rs` ≈1505–1555 / `directories.rs` ≈1181–1212 semantics),
   marked-row `marked_item_style` rendering, hover highlight via the
   shared `hovered_item(ctx.mouse_pos(), …)` helper (interaction.md
   ordering hover ≈ 0.58 < marked ≈ 0.65 > selection ≈ 0.50), and the
   dual-pane active-focus convention (the pane holding the keyboard
   cursor renders with the hover highlight even without the mouse —
   Radio/Playlists `focused_pane_selection_uses_the_hover_highlight`
   pattern). Tests: ctrl/alt/plain-click, marked + hover render,
   focused-pane.

**Closing state**: baseline `cargo test --release` **1293/1293** at
`5e58c29`, warnings 3 baseline. Feedback file + `notes.md` + `HANDOFF.md`
(current-state line + Pending entry) updated and committed on `working`
(host round-24 filing commit). Nothing implemented — isodev picks the
file up on the next pull.

## Round 24 — implementation (devcontainer, 2026-08-10)

Implemented both feedback items in the container (`working` @ `efeb87e`,
uncommitted — **needs host `cargo test --release` validation**):

1. **Esc-deselect everywhere.** Queue video list (`handle_video_action`)
   and the MPD tab (`directories.rs handle_action`) got the same Close arm
   as the audio queue / browser / search: `Close if !<marks>.is_empty()`
   → clear marks + drop the anchor + `event.consume()` + render, so the
   first Esc deselects and only a second Esc opens settings. The anchor
   drop uses a new `MarkState::clear_anchor()` (clears the anchor and the
   last range — shared by both panes). Shift-range re-anchor tests mirror
   the playlists ones for both panes (`shift_range_reanchors_after_esc_`
   `clears_the_selection`), plus `esc_with_a_selection_consumes_the_`
   `keypress` and `esc_without_a_selection_leaves_settings_enabled` —
   +6 tests (3 queue video, 3 directories).

2. **Search tab parity** (`search/mod.rs` + `search/inputs.rs`):
   - mouse multi-select on the results list in both phases: ctrl+click
     toggles the mark, alt+click ranges from the anchor (replacing the
     previous range), a plain click on another row clears the marks and
     re-anchors (queue/MPD semantics);
   - marked-row rendering via the existing `to_list_items` marked style,
     hover highlight on the row under the mouse via the shared
     `hovered_item` helper (hover applied on top, never over marked rows),
     and the list's `highlight_style` switches to the hover style when the
     mouse is over the selected row;
   - dual-pane focus: the results selection renders with the hover
     highlight in the BrowseResults phase and the focused filter input in
     the Search phase (`InputGroups` gained a `focused_style` + a
     `pane_focused` render flag) — the Radio/Playlists focus convention.
   - +5 tests: `ctrl_click_toggles_and_plain_click_clears_the_marks`,
     `alt_click_ranges_from_the_anchor`, `marked_rows_render_with_the_`
     `marked_style`, `row_under_mouse_gets_the_hover_highlight`,
     `focused_pane_selection_uses_the_hover_highlight`.

Docs synced: `frontend/interaction.md` (Esc rule, hover list, focused
panes, ctrl/alt-click list + date/source_files), `tabs/search-tab.md`
(multi-select + dual-pane interaction, date). `notes.md` + `HANDOFF.md`
updated (Pending → needs host validation).

**Closing state (container)**: tree-sitter parse clean on all 5 modified
files; **Rust build/tests NOT run (no toolchain in the container)** —
host: `cargo test --release` (expected 1293 + 11 new = 1304), warnings ≤
3 baseline, then commit + live check (Esc on queue video / MPD marked
sets, search ctrl/alt-click, hover, active-pane highlight).

## Round 24 — host validation (2026-08-10, closed host-side)

Container implementation pulled from `~/Projects/s2udio` (uncommitted
tree @ `efeb87e`, byte-identical) into `~/NJMRgit/s2udio-working` and
committed as `7e1f151` → pushed `origin/working`.

- **`cargo test --release`: 1304/1304** (expected 1293 + 11 new),
  `cargo build --release` 0 errors, **3 warnings = baseline**. All 11
  round-24 tests present (6 esc-deselect: queue-video + directories;
  5 search: ctrl/alt/plain-click, marked + hover render, focused-pane).
- **Binary installed**: `3ed57551` → `~/.local/bin/s2udio` (previous
  `87a5257a` backed up as `s2udio.bak-20260810-r24-pre`); old instance
  restarted on it.
- **Live-checked (agent-driven, socket remote + tmux)**: MPD tab and
  queue **Video** list (seeded a 2-entry persistent video playlist for
  the test, restored afterwards): with a marked set, the **first Esc
  clears the marks and does NOT open settings**; the **second Esc opens
  settings** — both new Close arms verified live. Search tab: live
  search runs (query "deadlife" → 14 results), filter input, results
  list, settings Esc-cycle all behave; the results-phase mouse paths
  (ctrl/alt-click, hover) are covered by the 5 unit tests (the
  Search↔BrowseResults phase transition is mouse-driven under the
  minimal keymap — no keyboard binding for Right/Left on the search
  tab, worth a look next round).
- **Environment green**: s2u-yt two-phase wrapper resolved a live
  probe (`jNQXAC9IVRw`), `s2u-yt-bgutil.service` minting PO tokens,
  mpDris2 + s2u-mpdris2 up.
- Housekeeping: stray untracked `.github/` in the dist checkout
  `~/NJMRgit/s2udio` (pre-restructure leftover, byte-identical to the
  full-tree workflows) trashed to `~/.trash/20260810-155938-…`;
  dist checkout clean.
- **User live-confirmation still pending** for the mouse parts
  (ctrl/alt-click multi-select, hover highlight, active-pane
  highlight) — ask the user to try them on the next restart.

## Round 25 — host-implemented fixes from user live-check (2026-08-10)

User live-check findings on the round-24 build:

1. **Search tab keyboard navigation**: `a`/`d`/`←`/`→` did not move
   between the filter pane and the results list (the minimal keymap binds
   those in the `directories` context, which the search pane never
   claimed). Fixed: the search pane now claims the directories actions in
   both phases — `d`/`→` enter the results (Search phase) / enqueue the
   highlighted result (BrowseResults), `a`/`←` return to the filters;
   `w`/`s` keep their navigation meaning (FolderUp/Down abandon to the
   common context). Live-verified (enqueue grows the MPD queue per
   `d`/`→` press, `a` returns to the filters).
2. **Search filter hover**: the filter inputs/spinner/button rows are
   clickable fields but had no hover feedback. Fixed in `inputs.rs`: the
   row under the mouse renders with the hover highlight (wins over the
   keyboard selection like the list rows; clears on keyboard input via
   the global pointer-drop).
3. **Video queue stale anchor (reported)**: the Esc→shift re-anchor was
   already live-verified correct (fresh range from the cursor in every
   scenario), but the removal paths were hardened anyway — marks-clearing
   now also drops the anchor: `video_remove_entries`, `clear_marked` and
   the audio Delete handlers call `clear_anchor()`/`clear_mark_anchor()`
   so a stale index can never survive a list renumber (the alt-click
   range path). If the user still sees a stale range after this build,
   they will provide the exact steps.

Tests: +3 (search keyboard phase switching, search filter hover, video
alt-click re-anchor after Esc) → **1307/1307**, binary warnings 3
baseline. Binary installed `2968ce39`.
